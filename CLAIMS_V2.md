# SEIRIS v2 — Defensible Claims

Konten arsitektur & keputusan sudah **dikonsolidasi** ke folder `agent-context/`:

- `agent-context/DOMAIN_LOGIC.md` — aturan domain & state machine
- `agent-context/DECISIONS.md` — ADR (kenapa X bukan Y)
- `agent-context/GOTCHAS.md` — trap & caveats yang sudah bikin bug

Baca folder `agent-context/` untuk versi terbaru yang **terverifikasi dari kode**.

> File ini sengaja dibiarkan sebagai pointer agar tautan lama tidak mati.
