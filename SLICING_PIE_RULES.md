# Slicing Pie — Aturan Penilaian Kontribusi

Dokumen ini sudah **dikonsolidasi** ke `agent-context/DOMAIN_LOGIC.md` (multiplier, formula `slice = FMV × multiplier`, dan arsitektur Slicing Pie Beranak).

Baca `agent-context/DOMAIN_LOGIC.md` untuk versi terbaru yang **terverifikasi dari kode** (termasuk koreksi: tipe `SALES` menggantikan legacy `REVENUE`).

> File ini sengaja dibiarkan sebagai pointer agar tautan lama tidak mati.
